# Restic environment — generated by Ansible — DO NOT EDIT MANUALLY
export RESTIC_REPOSITORY="s3:{{ s3_endpoint }}/{{ s3_bucket }}"
export RESTIC_PASSWORD="{{ restic_password }}"
export AWS_ACCESS_KEY_ID="{{ s3_access_key }}"
export AWS_SECRET_ACCESS_KEY="{{ s3_secret_key }}"
export VPS_ID="{{ vps_id }}"
export MASTER_URL="{{ master_url }}"
export BACKUP_FOLDERS="{{ backup_folders | join(' ') }}"
export TEAMSYSTEM_SERVICE="{{ teamsystem_service }}"
export RESTIC_KEEP_DAILY="{{ restic_keep_daily }}"
export RESTIC_KEEP_WEEKLY="{{ restic_keep_weekly }}"
