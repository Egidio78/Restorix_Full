version: "3.9"

services:
  master:
    build: ./master
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    env_file:
      - ./master/.env
    environment:
      - DB_PATH=/data/backup_monitor.db
